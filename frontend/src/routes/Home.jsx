import { React } from "react";
import { Link } from "react-router-dom";
import { PackViewer } from "../components";

const Home = () => {
    return (
        <div>
            <PackViewer />
        </div>
    );
}

export default Home;